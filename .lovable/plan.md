## Goal

Copy the existing nginx configuration from `/mnt/documents/business-card.nginx.conf` into the project repository so it is version-controlled alongside the rest of the codebase.

## Where to place it

The file will be copied to `selfhost/business-card.nginx.conf` in the repo (alongside the other self-hosting artifacts: `selfhost/package.json`, `selfhost/business-card.service`, etc.).

## What will change

- A new file `selfhost/business-card.nginx.conf` is created with the exact contents currently in `/mnt/documents/business-card.nginx.conf`.
- No other files are modified.
